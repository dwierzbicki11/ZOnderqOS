name: SMT stage 6 fixed IPI proof

on:
  push:
    branches: [ main ]
    paths: [ 'patches/cosmos-smt/**', 'tools/prepare-cosmos-smt.sh', 'tools/smoke-stage06-qemu.sh', 'ZonderqOS.csproj', 'Bootloader/limine.conf', 'source/**', '.github/workflows/smt-stage06.yml' ]
  pull_request:
    paths: [ 'patches/cosmos-smt/**', 'tools/prepare-cosmos-smt.sh', 'tools/smoke-stage06-qemu.sh', 'ZonderqOS.csproj', 'Bootloader/limine.conf', 'source/**', '.github/workflows/smt-stage06.yml' ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-stage06:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          - cpus: 1
            cores: 1
            threads: 1
          - cpus: 8
            cores: 4
            threads: 2
    steps:
      - uses: actions/checkout@v4
        with: { path: zonderq }
      - uses: actions/checkout@v4
        with:
          repository: CosmosOS/Cosmos
          ref: v3.0.85
          path: cosmos
          submodules: recursive
      - uses: actions/setup-dotnet@v4
        with: { dotnet-version: '10.0.x' }
      - name: Install native toolchain and QEMU
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-system-x86 lld xorriso nasm
          qemu-system-x86_64 --version
      - name: Validate and apply Stage 6
        env:
          ZONDERQ_COSMOS_SOURCE_ROOT: ${{ github.workspace }}/cosmos
        run: |
          set -o pipefail
          bash -n zonderq/tools/prepare-cosmos-smt.sh
          bash -n zonderq/tools/smoke-stage06-qemu.sh
          bash zonderq/tools/prepare-cosmos-smt.sh --through-stage 6 --check-only 2>&1 | tee zonderq/stage06-preflight.log
          bash zonderq/tools/prepare-cosmos-smt.sh --through-stage 6 --patch-only 2>&1 | tee zonderq/stage06-apply.log
          git -C cosmos diff --check
          git -C zonderq rev-parse HEAD > zonderq/stage06-revisions.log
          git -C cosmos rev-parse HEAD >> zonderq/stage06-revisions.log
          git -C cosmos diff --stat >> zonderq/stage06-revisions.log
      - name: Verify idempotence and build clean patched Cosmos packages
        env:
          ZONDERQ_COSMOS_SOURCE_ROOT: ${{ github.workspace }}/cosmos
        run: |
          set -o pipefail
          bash zonderq/tools/prepare-cosmos-smt.sh --through-stage 6 2>&1 | tee zonderq/stage06-packages.log
      - name: ARM64 managed HAL regression compile
        if: matrix.cpus == 1
        working-directory: cosmos
        run: |
          set -o pipefail
          dotnet build src/Cosmos.Kernel.HAL/Cosmos.Kernel.HAL.csproj \
            -c Release -r linux-arm64 -p:CosmosArch=arm64 \
            -p:GeneratePackageOnBuild=false --no-incremental 2>&1 | tee ../zonderq/stage06-arm64-hal.log
          dotnet build src/Cosmos.Kernel.HAL.ARM64/Cosmos.Kernel.HAL.ARM64.csproj \
            -c Release -r linux-arm64 -p:CosmosArch=arm64 \
            -p:GeneratePackageOnBuild=false --no-incremental 2>&1 | tee ../zonderq/stage06-arm64-platform.log
          echo 'ARM64 managed compile only; no ARM64 ISO boot/runtime claim.'
      - name: Force local patched Cosmos packages
        run: |
          cat > zonderq/NuGet.Config <<EOF
          <?xml version="1.0" encoding="utf-8"?>
          <configuration>
            <packageSources><clear /><add key="local-cosmos" value="${{ github.workspace }}/cosmos/artifacts/package/release" /><add key="nuget.org" value="https://api.nuget.org/v3/index.json" /></packageSources>
            <packageSourceMapping><packageSource key="local-cosmos"><package pattern="Cosmos.*" /></packageSource><packageSource key="nuget.org"><package pattern="*" /></packageSource></packageSourceMapping>
          </configuration>
          EOF
          mkdir -p "${{ runner.temp }}/zonderq-stage06-packages"
          test -z "$(ls -A "${{ runner.temp }}/zonderq-stage06-packages")"
      - name: Build real ZonderqOS x64 ISO
        working-directory: zonderq
        env:
          NUGET_PACKAGES: ${{ runner.temp }}/zonderq-stage06-packages
          LOCAL_COSMOS_FEED: ${{ github.workspace }}/cosmos/artifacts/package/release
        run: |
          set -o pipefail
          export PATH="$HOME/.dotnet/tools:$PATH"
          dotnet restore ZonderqOS.csproj -r linux-x64 -p:CosmosArch=x64 --configfile NuGet.Config --force --no-cache 2>&1 | tee stage06-restore.log
          python3 - <<'PY' | tee stage06-package-sources.log
          import json
          import os
          import pathlib

          cache = pathlib.Path(os.environ['NUGET_PACKAGES'])
          feed = pathlib.Path(os.environ['LOCAL_COSMOS_FEED']).resolve()
          required = {'cosmos.sdk', 'cosmos.kernel', 'cosmos.kernel.core', 'cosmos.kernel.core.x64', 'cosmos.kernel.hal', 'cosmos.kernel.hal.x64', 'cosmos.kernel.native.x64', 'cosmos.kernel.boot.limine'}
          found = set()
          for metadata in sorted(cache.glob('cosmos.*/3.0.85/.nupkg.metadata')):
              package = metadata.parent.parent.name
              source = json.loads(metadata.read_text())['source']
              if pathlib.Path(source).resolve() != feed:
                  raise SystemExit(f'[SMT6-CI][FAIL] {package} restored from {source}, expected {feed}')
              found.add(package)
              print(f'{package} 3.0.85: {source}')
          if required - found:
              raise SystemExit(f'[SMT6-CI][FAIL] missing local package metadata: {sorted(required - found)}')
          PY
          cosmos build -a x64 -v 2>&1 | tee stage06-build.log
          test -s output-x64/ZonderqOS.iso
          sha256sum output-x64/ZonderqOS.iso | tee stage06-iso.sha256
      - name: QEMU fixed IPI proof and 30-second stability
        working-directory: zonderq
        env:
          STAGE06_QEMU_CPU_MODEL: Nehalem
          STAGE06_QEMU_SOCKETS: 1
          STAGE06_QEMU_CPUS: ${{ matrix.cpus }}
          STAGE06_QEMU_CORES: ${{ matrix.cores }}
          STAGE06_QEMU_THREADS: ${{ matrix.threads }}
          STAGE06_QEMU_TIMEOUT_SECONDS: 90
          STAGE06_QEMU_STABLE_SECONDS: 30
        run: |
          set -o pipefail
          bash tools/smoke-stage06-qemu.sh output-x64/ZonderqOS.iso stage06-qemu-serial.log 2>&1 | tee stage06-smoke.log
      - name: Diagnostics
        if: always()
        working-directory: zonderq
        run: |
          tail -n 250 stage06-build.log 2>/dev/null || true
          cat stage06-qemu-serial.log stage06-qemu-serial.log.stderr 2>/dev/null || true
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: stage06-diagnostics-Nehalem-${{ matrix.cpus }}cpu
          if-no-files-found: ignore
          path: |
            zonderq/stage06-*.log
            zonderq/stage06-qemu-serial.log.stderr
            zonderq/stage06-iso.sha256
