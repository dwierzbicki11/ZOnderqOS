diff --git a/patches/cosmos-smt/0013-percpu-scheduler-foundation.patch b/patches/cosmos-smt/0013-percpu-scheduler-foundation.patch
index 4c0f91b..b89c653 100644
--- a/patches/cosmos-smt/0013-percpu-scheduler-foundation.patch
+++ b/patches/cosmos-smt/0013-percpu-scheduler-foundation.patch
@@ -1,21 +1,23 @@
 diff --git a/src/Cosmos.Kernel.Core/Bridge/Export/PerCpuSchedulerNative.cs b/src/Cosmos.Kernel.Core/Bridge/Export/PerCpuSchedulerNative.cs
 new file mode 100644
-index 000000000..4ecf5da48
+index 000000000..39f317560
 --- /dev/null
 +++ b/src/Cosmos.Kernel.Core/Bridge/Export/PerCpuSchedulerNative.cs
-@@ -0,0 +1,47 @@
+@@ -0,0 +1,50 @@
 +#if ARCH_X64
 +using System;
 +using System.Runtime.InteropServices;
 +using Cosmos.Kernel.Core.Runtime;
-+using Cosmos.Kernel.Core.Scheduler;
++using Cosmos.Kernel.Core.CPU;
 +
 +namespace Cosmos.Kernel.Core.Bridge;
 +
 +/// <summary>
-+/// Stage-13 managed entry used only to validate the per-CPU scheduler state
-+/// created during normal scheduler initialization. It does not schedule a
-+/// managed thread and returns to the native AP idle loop after one transition.
++/// Stage-13 managed entry used only to validate the AP's CPU-local identity.
++/// The BSP owns the managed PerCpuState enter/leave proof; an AP must not walk
++/// the scheduler object graph until a later stage gives it a managed thread
++/// and full runtime/GC participation. This entry returns to native idle after
++/// one bounded identity check.
 +/// </summary>
 +internal static partial class PerCpuSchedulerNative
 +{
@@ -27,18 +29,19 @@ index 000000000..4ecf5da48
 +        uint entryCount);
 +
 +    /// <summary>
-+    /// Performs one bounded enter/leave proof against the AP's dense
-+    /// scheduler CpuId and managed PerCpuState.
++    /// Performs one bounded CPU-local identity proof for the AP. The matching
++    /// managed PerCpuState lifecycle proof is performed by the BSP before the
++    /// native AP command is published.
 +    /// </summary>
 +    [UnmanagedCallersOnly(EntryPoint = "__cosmos_smp_percpu_scheduler_entry")]
 +    public static uint Entry(uint expectedCpuId)
 +    {
 +        StartupCodeHelpers.RhpReversePInvoke(IntPtr.Zero);
 +
-+        uint status = SchedulerManager.RunPerCpuSchedulerEntry(
-+            expectedCpuId,
-+            out uint phase,
-+            out uint entryCount);
++        uint cpuId = CpuLocal.CurrentCpuId;
++        uint status = cpuId == 0 || cpuId != expectedCpuId ? 1u : 0u;
++        uint phase = status == 0 ? 2u : 0u;
++        uint entryCount = status == 0 ? 1u : 0u;
 +
 +        if (status == 0 &&
 +            NativePublishResult(expectedCpuId, phase, entryCount) != 0)
@@ -52,7 +55,7 @@ index 000000000..4ecf5da48
 +}
 +#endif
 diff --git a/src/Cosmos.Kernel.Core/Scheduler/PerCpuState.cs b/src/Cosmos.Kernel.Core/Scheduler/PerCpuState.cs
-index 27ea1c851..2fb5233ab 100644
+index 27ea1c851..381c33539 100644
 --- a/src/Cosmos.Kernel.Core/Scheduler/PerCpuState.cs
 +++ b/src/Cosmos.Kernel.Core/Scheduler/PerCpuState.cs
 @@ -1,4 +1,5 @@
@@ -61,16 +64,17 @@ index 27ea1c851..2fb5233ab 100644
  
  namespace Cosmos.Kernel.Core.Scheduler;
  
-@@ -48,4 +49,41 @@ public sealed class PerCpuState : SchedulerExtensible
+@@ -48,4 +49,42 @@ public sealed class PerCpuState : SchedulerExtensible
  
      // ===== Synchronization =====
      internal SpinLock _lock;
 +
 +    // Stage 13 is deliberately a one-shot ownership handshake, not a
-+    // scheduler activation.  The AP proves that it can address this exact
-+    // managed PerCpuState, enters and leaves the state once, and then returns
-+    // to the native parked path.  A later stage will replace this proof with
-+    // real AP idle threads and dispatch/context-switch ownership.
++    // scheduler activation. The BSP validates this exact managed state and
++    // performs one enter/leave transition for it; the AP separately proves
++    // its CPU-local identity before returning to the native parked path. A
++    // later stage will replace this proof with real AP idle threads and
++    // dispatch/context-switch ownership.
 +    private int _stage13Phase;
 +    private int _stage13EntryCount;
 +
@@ -104,7 +108,7 @@ index 27ea1c851..2fb5233ab 100644
 +    }
  }
 diff --git a/src/Cosmos.Kernel.Core/Scheduler/SchedulerManager.cs b/src/Cosmos.Kernel.Core/Scheduler/SchedulerManager.cs
-index 9588e5fc6..c3103e0b6 100644
+index 9588e5fc6..fa685af4c 100644
 --- a/src/Cosmos.Kernel.Core/Scheduler/SchedulerManager.cs
 +++ b/src/Cosmos.Kernel.Core/Scheduler/SchedulerManager.cs
 @@ -15,7 +15,7 @@ namespace Cosmos.Kernel.Core.Scheduler;
@@ -144,60 +148,10 @@ index 9588e5fc6..c3103e0b6 100644
      /// <summary>
      /// Whether scheduler support is compiled into this kernel
      /// (the <c>CosmosEnableScheduler</c> feature switch). Internal: the ring
-@@ -276,6 +297,150 @@ public static class SchedulerManager
+@@ -276,6 +297,110 @@ public static class SchedulerManager
          set => s_enabled = value;
      }
  
-+    /// <summary>
-+    /// Validates one AP's addressability of its scheduler state and performs
-+    /// one enter/leave transition. This is deliberately not a dispatch,
-+    /// context switch, migration or preemption path: the AP returns to the
-+    /// native parked loop immediately after this bounded proof.
-+    /// </summary>
-+    internal static uint RunPerCpuSchedulerEntry(
-+        uint expectedCpuId,
-+        out uint phase,
-+        out uint entryCount)
-+    {
-+        phase = 0;
-+        entryCount = 0;
-+
-+        if (!s_enabled || s_currentScheduler is null || s_cpuStates is null)
-+        {
-+            return 1;
-+        }
-+
-+        if (expectedCpuId >= s_cpuCount || GetCurrentCpuId() != expectedCpuId)
-+        {
-+            return 2;
-+        }
-+
-+        PerCpuState state = s_cpuStates[expectedCpuId];
-+        if (state.CpuId != expectedCpuId ||
-+            state.IdleThread is null ||
-+            state.CurrentThread is null ||
-+            !ReferenceEquals(state.CurrentThread, state.IdleThread) ||
-+            state.CurrentThread.State != SchedulerThreadState.Running ||
-+            state.SchedulerData is null)
-+        {
-+            return 3;
-+        }
-+
-+        if (!state.TryEnterStage13(expectedCpuId))
-+        {
-+            return 4;
-+        }
-+
-+        if (!state.TryLeaveStage13(expectedCpuId))
-+        {
-+            return 5;
-+        }
-+
-+        phase = state.Stage13Phase;
-+        entryCount = state.Stage13EntryCount;
-+        return phase == 2 && entryCount == 1 ? 0u : 6u;
-+    }
-+
 +    /// <summary>
 +    /// Runs the Stage-13 per-CPU scheduler foundation proof after the
 +    /// scheduler policy and all per-CPU states have been initialized.
@@ -213,14 +167,24 @@ index 9588e5fc6..c3103e0b6 100644
 +            return false;
 +        }
 +
-+        PerCpuState bsp = s_cpuStates[0];
-+        if (bsp.CpuId != 0 || bsp.IdleThread is null ||
-+            !ReferenceEquals(bsp.CurrentThread, bsp.IdleThread) ||
-+            bsp.CurrentThread.State != SchedulerThreadState.Running ||
-+            bsp.SchedulerData is null)
++        for (uint cpuId = 0; cpuId < s_cpuCount; cpuId++)
 +        {
-+            Serial.WriteString("[SCHED] WARNING: Stage 13 BSP per-CPU state is invalid.\n");
-+            return false;
++            PerCpuState state = s_cpuStates[cpuId];
++            if (state.CpuId != cpuId || state.IdleThread is null ||
++                state.CurrentThread is null ||
++                !ReferenceEquals(state.CurrentThread, state.IdleThread) ||
++                state.CurrentThread.State != SchedulerThreadState.Running ||
++                state.SchedulerData is null || state.Stage13Phase != 0 ||
++                state.Stage13EntryCount != 0 ||
++                !state.TryEnterStage13(cpuId) ||
++                !state.TryLeaveStage13(cpuId) ||
++                state.Stage13Phase != 2 || state.Stage13EntryCount != 1)
++            {
++                Serial.WriteString("[SCHED] WARNING: Stage 13 per-CPU state is invalid for CpuId=");
++                Serial.WriteNumber(cpuId);
++                Serial.WriteString(".\n");
++                return false;
++            }
 +        }
 +
 +#if ARCH_X64
diff --git a/tools/prepare-cosmos-smt.sh b/tools/prepare-cosmos-smt.sh
index d3559e3..eaca670 100644
--- a/tools/prepare-cosmos-smt.sh
+++ b/tools/prepare-cosmos-smt.sh
@@ -376,6 +376,6 @@ elif (( SELECTED_STAGE == 12 )); then
     echo "[SMT] BSP waliduje wynik oraz brak pozostalej komendy; scheduler i managed Thread nadal nie sa wlaczane na AP-ach."
 elif (( SELECTED_STAGE == 13 )); then
     echo "[SMT] Etap 13 wykonuje jednorazowy handshake per-CPU z juz zainicjalizowanym schedulerem."
-    echo "[SMT] Kazdy AP waliduje wlasny PerCpuState, wykonuje enter/leave i wraca do native HLT."
+    echo "[SMT] BSP wykonuje enter/leave na kazdym PerCpuState, a kazdy AP waliduje swoja tozsamosc CPU-local i wraca do native HLT."
     echo "[SMT] Nie jest to jeszcze pelny scheduler SMP, migracja watkow ani preempcja."
 fi
diff --git a/tools/smoke-stage13-qemu.sh b/tools/smoke-stage13-qemu.sh
index 1a3ae20..fa9d457 100755
--- a/tools/smoke-stage13-qemu.sh
+++ b/tools/smoke-stage13-qemu.sh
@@ -56,7 +56,7 @@ for line in lines:
     if fatal.search(line) or re.search(r"\[SCHED\]\s*WARNING:", line, re.IGNORECASE):
         fail("kernel failure or scheduler warning: " + line)
 
-bsp_re = re.compile(r"\[SCHED-PERCPU\] cpu=0 phase=0 entries=0 state=bsp-idle")
+bsp_re = re.compile(r"\[SCHED-PERCPU\] cpu=0 phase=2 entries=1 state=bsp-idle")
 ap_re = re.compile(
     r"\[SCHED-PERCPU\] cpu=(\d+) phase=(\d+) entries=(\d+) "
     r"state=returned-native-idle"
