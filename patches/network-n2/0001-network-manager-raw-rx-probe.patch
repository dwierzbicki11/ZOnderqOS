diff --git a/src/Cosmos.Kernel.System/Network/NetworkManager.cs b/src/Cosmos.Kernel.System/Network/NetworkManager.cs
index 9a6f4d4..c4f7a11 100644
--- a/src/Cosmos.Kernel.System/Network/NetworkManager.cs
+++ b/src/Cosmos.Kernel.System/Network/NetworkManager.cs
@@ -111,3 +111,21 @@ public static class NetworkManager
         return IsEnabled && (PrimaryDevice?.Send(data, length) ?? false);
     }
+
+    /// <summary>
+    /// Installs a raw receive handler on the primary adapter. This is intended
+    /// for pre-IP bring-up and diagnostics; configuring the normal network
+    /// stack replaces this handler with its protocol dispatcher.
+    /// </summary>
+    /// <param name="handler">Callback invoked by the NIC for each received Ethernet frame.</param>
+    /// <returns>True when a primary device exists and the handler was installed.</returns>
+    public static bool SetRawReceiveHandler(Action<byte[], int> handler)
+    {
+        if (!IsEnabled || PrimaryDevice is null || handler is null)
+        {
+            return false;
+        }
+
+        PrimaryDevice.OnPacketReceived = (data, length) => handler(data, length);
+        return true;
+    }
 }
diff --git a/src/Cosmos.Kernel.System/PublicAPI.Unshipped.txt b/src/Cosmos.Kernel.System/PublicAPI.Unshipped.txt
index 89dff5a..ba6d4f1 100644
--- a/src/Cosmos.Kernel.System/PublicAPI.Unshipped.txt
+++ b/src/Cosmos.Kernel.System/PublicAPI.Unshipped.txt
@@ -1,2 +1,3 @@
 #nullable enable
+static Cosmos.Kernel.System.Network.NetworkManager.SetRawReceiveHandler(System.Action<byte[]!, int>! handler) -> bool
 abstract Cosmos.Kernel.System.Graphics.Canvas3D.ClearScene(System.Drawing.Color color, float depth = 1) -> void
